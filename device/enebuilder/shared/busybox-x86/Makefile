# Automatically generated by /enebuilder/busybox-1.32.1/scripts/mkmakefile: don't edit

VERSION = 1
PATCHLEVEL = 32

KERNELSRC    := /enebuilder/busybox-1.32.1
KERNELOUTPUT := /results

MAKEFLAGS += --no-print-directory

.PHONY: all $(MAKECMDGOALS)

all:
	$(MAKE) -C $(KERNELSRC) O=$(KERNELOUTPUT)

Makefile:;

$(filter-out all Makefile,$(MAKECMDGOALS)):
	$(MAKE) -C $(KERNELSRC) O=$(KERNELOUTPUT) $@

%/:
	$(MAKE) -C $(KERNELSRC) O=$(KERNELOUTPUT) $@
